# 软件配置

根据个人喜好，我会修改这几个选项：

- 每次启动都是新的窗口
快捷键 `ctrl+shift+p` 打开命令面板输入：`window:Restore Windows` 修改为 `none` 。

- 按住鼠标滚轮缩放字体
快捷键 `ctrl+shift+p` 打开命令面板输入：`Editor:Mouse Wheel Zoom` 勾选。

- 默认字体大小
快捷键 `ctrl+shift+p` 打开命令面板输入：`Editor:Font Size` 默认改为 18。