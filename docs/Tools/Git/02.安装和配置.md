# 安装&配置

安装直接无脑下一步即可。

初始化 Git
```bash
git config --global user.name 你的名字
git config --global user.email 你的邮箱
```

# 使用

在一个空的文件夹里执行 `git init` ，这个命令会初始化一个 Git 仓库。
并在仓库里创建 `.git` 文件夹。这个文件夹里存放了 Git 的所有数据。
默认是看不到这个文件夹的，可以通过 `git status` 命令查看当前仓库的状态。